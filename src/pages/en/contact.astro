---
/** Contact page — English. Rendered on demand, see the French route for why. */
import ContactView from '../../views/ContactView.astro';

export const prerender = false;
---

<ContactView locale="en" />
